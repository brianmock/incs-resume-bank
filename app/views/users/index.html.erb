<h3>Results:</h3>
<%= page_entries_info(@users, :model => "Candidate")%>

<% @users.each do |user| %>
<div class="result">
  <label>Name:</label> <%= user.prefix %> <%= user.first_name %> <%= user.last_name %><br>
  <label>Years of Experience: </label> <%= user.years %><br>
  <label>Licenses Held:</label> <%= user.licenses.map do |lic| lic.name end.join(', ') %><br>
  <label>Registered for 2019 Job Fair:</label> <% if user.register2019 == "both" || user.register2019 == "jobfaironly" %>Yes<% else %>No<%end%><br>
  <label>Location Preference:</label> <%= user.location_pref %><br>
  <label><a href="/users/<%= user.id %>">Profile</a></label><br>
</div>
<% end %>

<% if User.find(session[:user_id]).access == "admin" || User.find(session[:user_id]).access == "school"%>
  <%= form_tag(users_search_path(params.merge(format: 'csv'))) do %>
  <%= submit_tag 'Generate CSV', class: "btn btn-info export"%>
  <% end %>
<% end %>

<%= will_paginate @users %>
